const express=require('express')
const router=express.Router()
const cors=require('cors')
router.use(cors())
const {updateUserData,createOrder,getUserOrders,deleteOrder}=require('../controllers/userController')
const Authorization=require('../middlewares/authorisation')
router.patch('/updateUser',Authorization,updateUserData)
router.post('/createOrder',Authorization,createOrder)
router.get('/orders',Authorization,getUserOrders)
router.delete('/orders/delete/:orderID',Authorization,deleteOrder)
module.exports=router